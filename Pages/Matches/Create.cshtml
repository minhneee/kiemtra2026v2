@page
@model PickleballClubManagement.Pages.Matches.CreateModel
@{
    ViewData["Title"] = "Nhập Kết Quả Trận Đấu";
}

<style>
    * {
        --primary: #16A34A;
        --primary-dark: #15803D;
        --primary-light: #86EFAC;
        --success: #22C55E;
        --danger: #EF4444;
        --warning: #F59E0B;
        --gray-50: #F9FAFB;
        --gray-100: #F3F4F6;
        --gray-200: #E5E7EB;
        --gray-400: #9CA3AF;
        --gray-600: #4B5563;
        --gray-900: #111827;
    }

    .match-container {
        max-width: 900px;
        margin: 0 auto;
        padding: 2rem 1rem;
    }

    .page-header {
        background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
        color: white;
        padding: 2.5rem 2rem;
        border-radius: 16px;
        margin-bottom: 3rem;
        text-align: center;
        box-shadow: 0 10px 30px rgba(22, 163, 74, 0.15);
    }

    .page-header h1 {
        margin: 0;
        font-size: 2rem;
        font-weight: 800;
        letter-spacing: -0.5px;
    }

    .page-header p {
        margin: 0.5rem 0 0;
        opacity: 0.95;
        font-size: 0.95rem;
    }

    .wizard-container {
        background: white;
        border-radius: 16px;
        box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
        overflow: hidden;
    }

    .wizard-header {
        background: linear-gradient(90deg, var(--gray-50) 0%, white 100%);
        padding: 2rem;
        border-bottom: 2px solid var(--gray-200);
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 1rem;
    }

    .wizard-step {
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }

    .step-badge {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        background: var(--gray-200);
        color: var(--gray-600);
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        font-size: 0.9rem;
        flex-shrink: 0;
    }

    .step-label {
        font-size: 0.85rem;
        color: var(--gray-600);
        font-weight: 600;
    }

    .wizard-content {
        padding: 2rem;
    }

    .form-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 2rem;
        margin-bottom: 2rem;
    }

    .form-group-item {
        display: flex;
        flex-direction: column;
    }

    .form-group-item label {
        font-weight: 600;
        color: var(--gray-900);
        margin-bottom: 0.5rem;
        font-size: 0.95rem;
    }

    .format-toggle {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1rem;
        margin-bottom: 2rem;
    }

    .toggle-option {
        position: relative;
        cursor: pointer;
    }

    .toggle-option input[type="radio"] {
        display: none;
    }

    .toggle-label {
        display: block;
        padding: 1.5rem;
        border: 2px solid var(--gray-200);
        border-radius: 12px;
        text-align: center;
        transition: all 0.3s ease;
        background: white;
    }

    .toggle-option input[type="radio"]:checked ~ .toggle-label {
        background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
        color: white;
        border-color: var(--primary);
        box-shadow: 0 8px 20px rgba(22, 163, 74, 0.2);
    }

    .toggle-label:hover {
        border-color: var(--primary);
    }

    .toggle-emoji {
        font-size: 2rem;
        margin-bottom: 0.5rem;
    }

    .toggle-text {
        font-weight: 700;
        font-size: 1rem;
    }

    .vs-arena {
        background: linear-gradient(135deg, var(--gray-100) 0%, var(--gray-50) 100%);
        border-radius: 12px;
        padding: 2rem;
        margin: 2rem 0;
        display: grid;
        grid-template-columns: 1fr auto 1fr;
        gap: 2rem;
        align-items: flex-start;
    }

    .team-panel {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .team-header {
        font-weight: 700;
        padding-bottom: 0.75rem;
        border-bottom: 3px solid;
        font-size: 1rem;
    }

    .team-panel.winner .team-header {
        color: var(--success);
        border-bottom-color: var(--success);
    }

    .team-panel.loser .team-header {
        color: var(--danger);
        border-bottom-color: var(--danger);
    }

    .vs-center {
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 800;
        font-size: 1.5rem;
        color: var(--primary);
    }

    .select-wrapper {
        position: relative;
    }

    .select-wrapper select {
        width: 100%;
        padding: 0.75rem 1rem;
        border: 1px solid var(--gray-200);
        border-radius: 8px;
        background: white;
        font-size: 0.95rem;
        cursor: pointer;
        transition: all 0.2s ease;
    }

    .select-wrapper select:focus {
        outline: none;
        border-color: var(--primary);
        box-shadow: 0 0 0 3px rgba(22, 163, 74, 0.1);
    }

    .select-wrapper select:hover {
        border-color: var(--primary);
    }

    .option-section {
        background: var(--gray-50);
        padding: 1.5rem;
        border-radius: 12px;
        border: 1px solid var(--gray-200);
        margin-top: 1rem;
    }

    .option-item {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 1rem;
        background: white;
        border-radius: 8px;
        cursor: pointer;
        transition: all 0.2s ease;
        border: 1px solid var(--gray-200);
    }

    .option-item:hover {
        background: var(--gray-50);
        border-color: var(--primary);
    }

    .option-item input[type="checkbox"] {
        width: 20px;
        height: 20px;
        cursor: pointer;
        accent-color: var(--primary);
    }

    .option-content {
        flex: 1;
    }

    .option-title {
        font-weight: 700;
        color: var(--gray-900);
        margin-bottom: 0.25rem;
    }

    .option-desc {
        font-size: 0.85rem;
        color: var(--gray-600);
    }

    .action-buttons {
        display: flex;
        gap: 1rem;
        justify-content: flex-end;
        margin-top: 2rem;
        padding-top: 2rem;
        border-top: 1px solid var(--gray-200);
    }

    .btn {
        padding: 0.75rem 2rem;
        border-radius: 8px;
        font-weight: 600;
        font-size: 0.95rem;
        cursor: pointer;
        transition: all 0.2s ease;
        border: none;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
    }

    .btn-primary {
        background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
        color: white;
        box-shadow: 0 4px 12px rgba(22, 163, 74, 0.25);
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(22, 163, 74, 0.35);
    }

    .btn-secondary {
        background: var(--gray-100);
        color: var(--gray-900);
        border: 1px solid var(--gray-200);
    }

    .btn-secondary:hover {
        background: var(--gray-200);
    }

    @@media (max-width: 768px) {
        .match-container {
            padding: 1rem;
        }

        .page-header {
            padding: 1.5rem 1rem;
        }

        .page-header h1 {
            font-size: 1.5rem;
        }

        .wizard-header {
            grid-template-columns: repeat(2, 1fr);
            gap: 0.75rem;
            padding: 1.5rem;
        }

        .wizard-content {
            padding: 1.5rem;
        }

        .form-grid {
            grid-template-columns: 1fr;
            gap: 1rem;
        }

        .vs-arena {
            grid-template-columns: 1fr;
            gap: 1rem;
            padding: 1.5rem;
        }

        .vs-center {
            padding: 1rem 0;
        }

        .action-buttons {
            flex-direction: column-reverse;
        }

        .action-buttons .btn {
            width: 100%;
            justify-content: center;
        }
    }
</style>

<div class="match-container">
    <div class="page-header">
        <h1>Ghi Nhận Kết Quả</h1>
        <p>Cập nhật điểm xếp hạng và lịch sử đấu</p>
    </div>

    <div class="wizard-container">
        <div class="wizard-header">
            <div class="wizard-step">
                <div class="step-badge">1</div>
                <div class="step-label">Hình Thức</div>
            </div>
            <div class="wizard-step">
                <div class="step-badge">2</div>
                <div class="step-label">Kèo</div>
            </div>
            <div class="wizard-step">
                <div class="step-badge">3</div>
                <div class="step-label">Cầu Thủ</div>
            </div>
            <div class="wizard-step">
                <div class="step-badge">4</div>
                <div class="step-label">Tùy Chọn</div>
            </div>
        </div>

        <div class="wizard-content">
            <form method="post" id="matchForm">
                <!-- Hình Thức -->
                <div style="margin-bottom: 3rem;">
                    <label style="display: block; font-weight: 700; margin-bottom: 1rem; color: var(--gray-900);">
                        Chọn hình thức đấu
                    </label>
                    <div class="format-toggle">
                        <label class="toggle-option">
                            <input type="radio" asp-for="Input.Format" value="0" checked />
                            <div class="toggle-label">
                                <div class="toggle-emoji">👤</div>
                                <div class="toggle-text">ĐƠN</div>
                                <div style="font-size: 0.8rem; color: inherit; opacity: 0.8; margin-top: 0.25rem;">1 vs 1</div>
                            </div>
                        </label>
                        <label class="toggle-option">
                            <input type="radio" asp-for="Input.Format" value="1" />
                            <div class="toggle-label">
                                <div class="toggle-emoji">👥</div>
                                <div class="toggle-text">ĐÔI</div>
                                <div style="font-size: 0.8rem; color: inherit; opacity: 0.8; margin-top: 0.25rem;">2 vs 2</div>
                            </div>
                        </label>
                    </div>
                </div>

                <!-- Kèo -->
                <div style="margin-bottom: 3rem;">
                    <label style="display: block; font-weight: 700; margin-bottom: 1rem; color: var(--gray-900);">
                        Kèo liên quan
                    </label>
                    <div class="select-wrapper">
                        <select asp-for="Input.ChallengeId">
                            <option value="">Đánh tự do (không có kèo)</option>
                            @foreach (var challenge in Model.AcceptedChallenges)
                            {
                                <option value="@challenge.Id">@challenge.Title (@challenge.Creator?.FullName)</option>
                            }
                        </select>
                    </div>
                </div>

                <!-- Đội chơi -->
                <div style="margin-bottom: 2rem;">
                    <label style="display: block; font-weight: 700; margin-bottom: 1.5rem; color: var(--gray-900);">
                        Chọn cầu thủ
                    </label>
                    <div class="vs-arena">
                        <div class="team-panel winner">
                            <div class="team-header">🎖️ ĐỘI THẮNG</div>
                            <div class="select-wrapper">
                                <select asp-for="Input.Winner1Id" required>
                                    <option value="">Cầu thủ chính</option>
                                    @foreach (var member in Model.Members)
                                    {
                                        <option value="@member.Id">@member.FullName</option>
                                    }
                                </select>
                            </div>
                            <div class="select-wrapper" id="winner2Group" style="display: none;">
                                <select asp-for="Input.Winner2Id">
                                    <option value="">Cầu thủ thứ 2</option>
                                    @foreach (var member in Model.Members)
                                    {
                                        <option value="@member.Id">@member.FullName</option>
                                    }
                                </select>
                            </div>
                        </div>

                        <div class="vs-center">VS</div>

                        <div class="team-panel loser">
                            <div class="team-header">⬇️ ĐỘI THUA</div>
                            <div class="select-wrapper">
                                <select asp-for="Input.Loser1Id" required>
                                    <option value="">Cầu thủ chính</option>
                                    @foreach (var member in Model.Members)
                                    {
                                        <option value="@member.Id">@member.FullName</option>
                                    }
                                </select>
                            </div>
                            <div class="select-wrapper" id="loser2Group" style="display: none;">
                                <select asp-for="Input.Loser2Id">
                                    <option value="">Cầu thủ thứ 2</option>
                                    @foreach (var member in Model.Members)
                                    {
                                        <option value="@member.Id">@member.FullName</option>
                                    }
                                </select>
                            </div>
                        </div>
                    </div>
                    <div asp-validation-summary="ModelOnly" class="text-danger mt-2"></div>
                </div>

                <!-- Tỷ Số -->
                <div style="margin-bottom: 2rem;">
                    <label style="display: block; font-weight: 700; margin-bottom: 1.5rem; color: var(--gray-900);">
                        Nhập tỷ số
                    </label>
                    <div style="background: linear-gradient(135deg, var(--gray-100) 0%, var(--gray-50) 100%); border-radius: 12px; padding: 2rem;">
                        <!-- Set 1 -->
                        <div style="margin-bottom: 1.5rem; padding-bottom: 1.5rem; border-bottom: 1px solid var(--gray-200);">
                            <div style="font-weight: 700; color: var(--gray-900); margin-bottom: 1rem; font-size: 1rem;">
                                🎯 Set 1 <span style="color: var(--gray-600); font-weight: 500;">(Bắt buộc)</span>
                            </div>
                            <div style="display: grid; grid-template-columns: 1fr auto 1fr; gap: 1rem; align-items: flex-end;">
                                <div>
                                    <label style="display: block; font-weight: 600; color: var(--success); margin-bottom: 0.5rem; font-size: 0.9rem;">
                                        Đội Thắng
                                    </label>
                                    <input type="number" asp-for="Input.Set1WinnerScore" min="0" placeholder="0" 
                                        style="width: 100%; padding: 0.75rem; border: 1px solid var(--gray-200); border-radius: 8px; font-size: 1.1rem; font-weight: 700; text-align: center;" required />
                                </div>
                                <div style="text-align: center; color: var(--gray-600); font-weight: 700; font-size: 1.2rem;">-</div>
                                <div>
                                    <label style="display: block; font-weight: 600; color: var(--danger); margin-bottom: 0.5rem; font-size: 0.9rem;">
                                        Đội Thua
                                    </label>
                                    <input type="number" asp-for="Input.Set1LoserScore" min="0" placeholder="0" 
                                        style="width: 100%; padding: 0.75rem; border: 1px solid var(--gray-200); border-radius: 8px; font-size: 1.1rem; font-weight: 700; text-align: center;" required />
                                </div>
                            </div>
                        </div>

                        <!-- Set 2 -->
                        <div style="margin-bottom: 1.5rem; padding-bottom: 1.5rem; border-bottom: 1px solid var(--gray-200);">
                            <div style="font-weight: 700; color: var(--gray-900); margin-bottom: 1rem; font-size: 1rem;">
                                🎯 Set 2 <span style="color: var(--gray-600); font-weight: 500;">(Tùy chọn)</span>
                            </div>
                            <div style="display: grid; grid-template-columns: 1fr auto 1fr; gap: 1rem; align-items: flex-end;">
                                <div>
                                    <label style="display: block; font-weight: 600; color: var(--success); margin-bottom: 0.5rem; font-size: 0.9rem;">
                                        Đội Thắng
                                    </label>
                                    <input type="number" asp-for="Input.Set2WinnerScore" min="0" placeholder="0" 
                                        style="width: 100%; padding: 0.75rem; border: 1px solid var(--gray-200); border-radius: 8px; font-size: 1.1rem; font-weight: 700; text-align: center;" />
                                </div>
                                <div style="text-align: center; color: var(--gray-600); font-weight: 700; font-size: 1.2rem;">-</div>
                                <div>
                                    <label style="display: block; font-weight: 600; color: var(--danger); margin-bottom: 0.5rem; font-size: 0.9rem;">
                                        Đội Thua
                                    </label>
                                    <input type="number" asp-for="Input.Set2LoserScore" min="0" placeholder="0" 
                                        style="width: 100%; padding: 0.75rem; border: 1px solid var(--gray-200); border-radius: 8px; font-size: 1.1rem; font-weight: 700; text-align: center;" />
                                </div>
                            </div>
                        </div>

                        <!-- Set 3 -->
                        <div>
                            <div style="font-weight: 700; color: var(--gray-900); margin-bottom: 1rem; font-size: 1rem;">
                                🎯 Set 3 <span style="color: var(--gray-600); font-weight: 500;">(Tùy chọn)</span>
                            </div>
                            <div style="display: grid; grid-template-columns: 1fr auto 1fr; gap: 1rem; align-items: flex-end;">
                                <div>
                                    <label style="display: block; font-weight: 600; color: var(--success); margin-bottom: 0.5rem; font-size: 0.9rem;">
                                        Đội Thắng
                                    </label>
                                    <input type="number" asp-for="Input.Set3WinnerScore" min="0" placeholder="0" 
                                        style="width: 100%; padding: 0.75rem; border: 1px solid var(--gray-200); border-radius: 8px; font-size: 1.1rem; font-weight: 700; text-align: center;" />
                                </div>
                                <div style="text-align: center; color: var(--gray-600); font-weight: 700; font-size: 1.2rem;">-</div>
                                <div>
                                    <label style="display: block; font-weight: 600; color: var(--danger); margin-bottom: 0.5rem; font-size: 0.9rem;">
                                        Đội Thua
                                    </label>
                                    <input type="number" asp-for="Input.Set3LoserScore" min="0" placeholder="0" 
                                        style="width: 100%; padding: 0.75rem; border: 1px solid var(--gray-200); border-radius: 8px; font-size: 1.1rem; font-weight: 700; text-align: center;" />
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Tùy Chọn -->
                <div class="option-section">
                    <label class="option-item">
                        <input type="checkbox" asp-for="Input.IsRanked" checked />
                        <div class="option-content">
                            <div class="option-title">Tính Điểm Ranking</div>
                            <div class="option-desc">Tự động cập nhật điểm xếp hạng: +0.1 thắng, -0.1 thua</div>
                        </div>
                    </label>
                </div>

                <div class="action-buttons">
                    <a href="/" class="btn btn-secondary">
                        <span>←</span>
                        <span>Trở lại</span>
                    </a>
                    <button type="submit" class="btn btn-primary">
                        <span>✓</span>
                        <span>Lưu Kết Quả</span>
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Show/hide player 2 fields based on format
        const formatRadios = document.querySelectorAll('input[name="Input.Format"]');
        const winner2Group = document.getElementById('winner2Group');
        const loser2Group = document.getElementById('loser2Group');

        function updatePlayerFields() {
            const isSingles = document.querySelector('input[name="Input.Format"]:checked').value === '0';
            winner2Group.style.display = isSingles ? 'none' : 'block';
            loser2Group.style.display = isSingles ? 'none' : 'block';
            
            if (isSingles) {
                document.querySelector('select[name="Input.Winner2Id"]').value = '';
                document.querySelector('select[name="Input.Loser2Id"]').value = '';
            }
        }

        formatRadios.forEach(radio => {
            radio.addEventListener('change', updatePlayerFields);
        });

        // Validation: Check for duplicate players
        document.getElementById('matchForm').addEventListener('submit', function(e) {
            const winner1 = document.querySelector('select[name="Input.Winner1Id"]').value;
            const winner2 = document.querySelector('select[name="Input.Winner2Id"]').value;
            const loser1 = document.querySelector('select[name="Input.Loser1Id"]').value;
            const loser2 = document.querySelector('select[name="Input.Loser2Id"]').value;

            const players = [winner1, winner2, loser1, loser2].filter(p => p !== '');
            const uniquePlayers = new Set(players);

            if (players.length !== uniquePlayers.size) {
                e.preventDefault();
                alert('⚠️ Lỗi: Một người không thể vừa thắng vừa thua, hoặc xuất hiện nhiều lần!');
                return false;
            }
        });
    </script>
    <partial name="_ValidationScriptsPartial" />
}
