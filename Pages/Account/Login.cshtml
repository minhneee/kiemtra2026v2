@page
@model LoginModel

@{
    ViewData["Title"] = "Đăng Nhập";
}

<style>
    .auth-wrapper {
        display: grid;
        grid-template-columns: 1fr 1fr;
        min-height: calc(100vh - 80px);
        gap: 0;
    }

    .auth-hero {
        background: linear-gradient(135deg, #16A34A 0%, #22C55E 100%);
        padding: 4rem;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        color: white;
        position: relative;
        overflow: hidden;
    }

    .auth-hero::before {
        content: '';
        position: absolute;
        top: -50%;
        right: -10%;
        width: 600px;
        height: 600px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 50%;
    }

    .auth-hero-content {
        position: relative;
        z-index: 1;
        text-align: center;
        max-width: 400px;
    }

    .auth-hero-icon {
        font-size: 6rem;
        margin-bottom: 2rem;
    }

    .auth-hero h2 {
        font-size: 2.5rem;
        font-weight: 900;
        margin-bottom: 1rem;
        line-height: 1.2;
    }

    .auth-hero p {
        font-size: 1.2rem;
        opacity: 0.95;
        margin-bottom: 2rem;
        line-height: 1.6;
    }

    .auth-hero-features {
        background: rgba(255, 255, 255, 0.15);
        backdrop-filter: blur(10px);
        padding: 2rem;
        border-radius: 16px;
        border: 1px solid rgba(255, 255, 255, 0.3);
    }

    .feature-item {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-bottom: 1rem;
        font-size: 1.05rem;
    }

    .feature-item:last-child {
        margin-bottom: 0;
    }

    .feature-check {
        font-size: 1.5rem;
    }

    .auth-form-wrapper {
        padding: 4rem;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .auth-form-header {
        margin-bottom: 3rem;
    }

    .auth-form-header h1 {
        font-size: 2.2rem;
        font-weight: 900;
        color: var(--color-gray-900);
        margin-bottom: 0.5rem;
    }

    .auth-form-header p {
        color: var(--color-gray-600);
        font-size: 1.1rem;
    }

    .demo-box {
        background: linear-gradient(135deg, rgba(22, 163, 74, 0.08) 0%, rgba(34, 197, 94, 0.08) 100%);
        border-left: 4px solid var(--color-primary);
        padding: 1.5rem;
        border-radius: 12px;
        margin-bottom: 2rem;
    }

    .demo-box strong {
        display: block;
        color: var(--color-primary);
        margin-bottom: 0.8rem;
        font-size: 1.05rem;
    }

    .demo-item {
        font-size: 0.95rem;
        color: var(--color-gray-700);
        margin-bottom: 0.5rem;
    }

    .demo-item code {
        background: rgba(0, 0, 0, 0.05);
        padding: 2px 8px;
        border-radius: 4px;
        font-family: 'Courier New', monospace;
        font-weight: 600;
    }

    .form-group {
        margin-bottom: 1.5rem;
    }

    .form-label {
        display: block;
        margin-bottom: 0.5rem;
        font-weight: 700;
        color: var(--color-gray-800);
    }

    .form-control {
        width: 100%;
        padding: 0.875rem 1rem;
        border: 2px solid var(--color-gray-300);
        border-radius: var(--radius-md);
        font-size: 1rem;
        transition: var(--transition-base);
    }

    .form-control:focus {
        outline: none;
        border-color: var(--color-primary);
        box-shadow: 0 0 0 4px rgba(22, 163, 74, 0.1);
    }

    .form-check {
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }

    .form-check input {
        width: 18px;
        height: 18px;
        cursor: pointer;
        accent-color: var(--color-primary);
    }

    .form-check label {
        margin: 0;
        cursor: pointer;
        color: var(--color-gray-700);
    }

    .btn {
        width: 100%;
        padding: 1rem;
        background: var(--gradient-primary);
        color: white;
        border: none;
        border-radius: var(--radius-md);
        font-size: 1.05rem;
        font-weight: 700;
        cursor: pointer;
        transition: var(--transition-base);
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-top: 0.5rem;
    }

    .btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 24px rgba(22, 163, 74, 0.3);
    }

    .divider {
        text-align: center;
        margin: 2rem 0;
        padding: 2rem 0;
        border-top: 1px solid var(--color-gray-300);
        color: var(--color-gray-600);
        font-size: 0.95rem;
    }

    .auth-link {
        display: flex;
        gap: 0.5rem;
    }

    .auth-link a {
        color: var(--color-primary);
        text-decoration: none;
        font-weight: 700;
        transition: var(--transition-base);
    }

    .auth-link a:hover {
        text-decoration: underline;
    }

    .error-message {
        background: rgba(220, 38, 38, 0.08);
        border-left: 4px solid #DC2626;
        padding: 1rem;
        border-radius: 8px;
        color: #991B1B;
        margin-bottom: 1.5rem;
        font-size: 0.95rem;
    }

    .text-danger {
        color: #DC2626;
        font-size: 0.875rem;
        margin-top: 0.25rem;
        display: block;
    }

    @@media (max-width: 1024px) {
        .auth-wrapper {
            grid-template-columns: 1fr;
        }

        .auth-hero {
            min-height: 250px;
            padding: 3rem 2rem;
        }

        .auth-hero h2 {
            font-size: 2rem;
        }

        .auth-form-wrapper {
            padding: 3rem 2rem;
        }
    }

    @@media (max-width: 768px) {
        .auth-form-wrapper {
            padding: 2rem 1.5rem;
        }

        .auth-form-header h1 {
            font-size: 1.8rem;
        }

        .demo-box {
            padding: 1rem;
        }
    }
</style>

<div class="auth-wrapper">
    <div class="auth-hero">
        <div class="auth-hero-content">
            <div class="auth-hero-icon">🏓</div>
            <h2>Chào Mừng Trở Lại!</h2>
            <p>Đăng nhập để tiếp tục hành trình pickleball của bạn</p>
            
            <div class="auth-hero-features">
                <div class="feature-item">
                    <span class="feature-check">✓</span>
                    <span>Tìm đối thủ xứng tầm</span>
                </div>
                <div class="feature-item">
                    <span class="feature-check">✓</span>
                    <span>Đặt sân và thi đấu</span>
                </div>
                <div class="feature-item">
                    <span class="feature-check">✓</span>
                    <span>Theo dõi thống kê</span>
                </div>
            </div>
        </div>
    </div>

    <div class="auth-form-wrapper">
        <div class="auth-form-header">
            <h1>Đăng Nhập</h1>
            <p>Nhập thông tin của bạn để vào tài khoản</p>
        </div>

        <div class="demo-box">
            <strong>🔑 Tài Khoản Demo:</strong>
            <div class="demo-item">Tên đăng nhập: <code>admin</code></div>
            <div class="demo-item">Mật khẩu: <code>admin123</code></div>
        </div>

        <form method="post">
            @{
                var errors = ViewData.ModelState;
                if (errors.ContainsKey("") && errors[""]?.Errors.Count > 0)
                {
                    <div class="error-message">
                        @errors[""]?.Errors[0].ErrorMessage
                    </div>
                }
            }

            <div class="form-group">
                <label class="form-label" asp-for="Input.Username">👤 Tên Đăng Nhập</label>
                <input class="form-control" asp-for="Input.Username" autocomplete="username" placeholder="your-username" />
                <span asp-validation-for="Input.Username" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label class="form-label" asp-for="Input.Password">🔐 Mật Khẩu</label>
                <input class="form-control" asp-for="Input.Password" type="password" autocomplete="current-password" placeholder="••••••••" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>

            <div class="form-group">
                <div class="form-check">
                    <input class="form-check-input" asp-for="Input.RememberMe" id="remember" />
                    <label for="remember" class="form-check label">Ghi nhớ đăng nhập</label>
                </div>
            </div>

            <button type="submit" class="btn">🚀 Đăng Nhập</button>
        </form>

        <div class="divider">Chưa có tài khoản?</div>

        <div class="auth-link">
            <span>Tạo tài khoản mới</span>
            <a href="/Account/Register">Đăng Ký Ngay →</a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
